#!/usr/bin/env python
# -*- coding: utf-8 -*-

# All package metadata lives in setup.cfg; the version is the single source of
# truth in icinga2apic/__init__.py (setup.cfg reads it via `attr:`).
from setuptools import setup

setup()
